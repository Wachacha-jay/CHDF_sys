-- Temporarily disabled due to MySQL dynamic SQL restrictions in stored functions
-- The logic will be handled via the service layer or fixed stored procedures later.

/*
CREATE FUNCTION generate_next_code(prefix VARCHAR(20), table_name VARCHAR(50))
...
*/
